package com.example.androidtask.api;

/**
 * A class to be parsed into the error type from Github APIS. i only need the message to display to users
 */
public class ErrorResponse {
    public String message;
}
